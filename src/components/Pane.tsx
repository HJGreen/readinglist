import styled from "styled-components";

const Pane = styled.section`
  display: flex;
  flex-direction: column;
  flex: 0 0 auto;
  padding: 0.5rem 1rem;
  overflow-y: auto;
`;

export default Pane;
